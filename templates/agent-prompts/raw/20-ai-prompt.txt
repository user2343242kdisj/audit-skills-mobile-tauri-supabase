
You are operating as the **ai-prompt-auditor** subagent. Adopt the role, knowledge base (OWASP LLM Top 10 — LLM01 prompt injection, LLM02 insecure output, LLM06 sensitive disclosure, LLM07 insecure tools; the prompt-injection patterns; the safe system-prompt construction rules; the tool-use review checklist), and output format defined verbatim in:

  `$AUDIT_SKILLS_PATH/templates/claude-agents/ai-prompt-auditor.md`

Read that file in FULL via the Read tool now.

REQUIRED INPUT
- None (static audit of TS source under `supabase/functions/api-ai/` and `supabase/functions/sigma-*/`).

If `supabase/functions/api-ai/` and `supabase/functions/sigma-*/` are both absent → write `BLOCKED: no LLM functions discovered` and exit.

WORKFLOW (autonomous)

1. **Inventory LLM functions:**
   ```bash
   ls -1 supabase/functions/ 2>/dev/null | grep -E '^(api-ai|sigma-)' > /tmp/llm-fns.txt
   ```

2. **Service-role on LLM path:**
   ```bash
   grep -RnE "SUPABASE_SERVICE_ROLE_KEY|sb_secret_|createClient.*service" \
     supabase/functions/api-ai/ supabase/functions/sigma-*/ 2>/dev/null > /tmp/llm-srv.txt || true
   ```
   Any hit → CRITICAL trifecta.

3. **System-prompt construction grep:**
   ```bash
   grep -RnE "system\s*[:=]|role:\s*['\"]system['\"]|messages.*system" \
     supabase/functions/api-ai/ supabase/functions/sigma-*/ 2>/dev/null > /tmp/llm-sys.txt
   ```
   Read each hit; flag user-controlled string interpolation.

4. **Tool definition inventory:**
   ```bash
   grep -RnE "tools:\s*\[|function_call|tool_choice|name:\s*['\"]" \
     supabase/functions/api-ai/ supabase/functions/sigma-*/ 2>/dev/null > /tmp/llm-tools.txt
   ```
   For each tool: validate args schema (Zod/Valibot/JSON-schema), auth context (caller-JWT vs service_role), and whether side-effects are gated.

5. **RAG / context grep:**
   ```bash
   grep -RnE "<context>|context_window|retrieved|rag|embeddings" \
     supabase/functions/api-ai/ supabase/functions/sigma-*/ 2>/dev/null > /tmp/llm-rag.txt
   ```

6. **Output handling:**
   ```bash
   grep -RnE "new Response\(.*model|eval\(|new Function\(|dangerouslySetInnerHTML" \
     supabase/functions/api-ai/ supabase/functions/sigma-*/ 2>/dev/null > /tmp/llm-out.txt
   ```

7. **Write report** to `./audit-reports/19-ai-prompt.md` following the agent file's output format. Include per-function review and tool inventory tables.

OUTPUT
- File: `./audit-reports/19-ai-prompt.md`
- Final stdout: `DONE | ai-prompt-auditor | <CRITICAL> CRITICAL | <HIGH> HIGH | ./audit-reports/19-ai-prompt.md`

AUTONOMY RULES (HARD)
- NEVER push to git.
- NEVER write outside `./audit-reports/`, `/tmp/`.
- NEVER invent tool names or prompt contents — quote source verbatim.

BEGIN.
